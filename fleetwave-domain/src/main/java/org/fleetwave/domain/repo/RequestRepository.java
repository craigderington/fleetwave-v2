package org.fleetwave.domain.repo; import org.fleetwave.domain.Request; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface RequestRepository extends JpaRepository<Request, java.util.UUID>{ List<Request> findByRequesterId(java.util.UUID requesterId);}